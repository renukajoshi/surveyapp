<%= form_for(@given_answer) do |f| %>
  <% if @given_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@given_answer.errors.count, "error") %> prohibited this given_answer from being saved:</h2>

      <ul>
      <% @given_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :survey_id %><br>
    <%= f.number_field :survey_id %>
  </div>
  <div class="field">
    <%= f.label :question_id %><br>
    <%= f.number_field :question_id %>
  </div>
  <div class="field">
    <%= f.label :answer %><br>
    <%= f.text_area :answer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
