<!DOCTYPE html>
<html>
<head>
  <title>SurveyApp</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="css/bootstrap.css">
  <link href="assets/css/bootstrap-responsive.css" rel="stylesheet">
  <!--Link to include glyphicon in rails -->
  <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
</head>
<header>
	<h3>Survey</h3>
</header>
<p class="navbar-text pull-right" align="right">  
<% if user_signed_in? %>
  Logged in as <strong><%= current_user.email%></strong>.
  <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link',class: "btn btn-link" %> |
  <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-link"  %>
<% else %>
  <%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link',class: "btn btn-link"  %> |
  <%= link_to "Login", new_user_session_path, :class => 'navbar-link',class: "btn btn-link"  %>

<% end %>

<% if notice %>
  <p class="alert alert-success"><%= notice %></p>
<% end %>
<% if alert %>
  <p class="alert alert-danger"><%= alert %></p>
<% end %>

<body>

<div class="container">

<%= yield %>

</div>
</body>
</html>
